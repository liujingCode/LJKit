use_frameworks!

platform :ios, '9.0'

target 'LJKit_Example' do
  
#  pod 'LJKit', :git => 'https://github.com/liujingCode/LJKit.git'

#  pod 'LJKit/Utils/HttpManager', :path => '../'

  pod 'YYModel'
  # 布局
  pod 'Masonry',:inhibit_warnings => true
  # 图片下载
  pod 'SDWebImage',:inhibit_warnings => true
  # 网络请求
  pod 'AFNetworking'
  # 下拉刷新
  pod 'MJRefresh',:inhibit_warnings => true
  # 字典转模型
  pod 'MJExtension'
  # 空列表
  pod 'DZNEmptyDataSet',:inhibit_warnings => true
  # 键盘
  pod 'IQKeyboardManager'
  # 轮播图
  pod 'SDCycleScrollView',:inhibit_warnings => true
  # toast
  pod 'MBProgressHUD', '~> 1.1.0'
  # 相册选择器
  pod 'TZImagePickerController'
  # 二维码扫描
  pod 'ZXingObjC', '~> 3.6.4'
  # 内嵌web服务器
  pod "GCDWebServer"
  pod "GCDWebServer/WebUploader"
  
  
  # 滴滴项目调试框架
#  pod 'DoraemonKit/Core', '~> 2.0.0', :configurations => ['Debug']
#  pod 'DoraemonKit/WithLogger', '~> 2.0.0', :configurations => ['Debug']
#  pod 'DoraemonKit/WithGPS', '~> 2.0.0', :configurations => ['Debug']
#  pod 'DoraemonKit/WithLoad', '~> 2.0.0', :configurations => ['Debug']
#  pod 'DoraemonKit/WithWeex', '~> 2.0.0', :configurations => ['Debug']
#  pod 'DoraemonKit/WithDatabase', '~> 2.0.0', :configurations => ['Debug']
#  pod 'DoraemonKit/WithMLeaksFinder', '2.0.0', :configurations => ['Debug']
  
  target 'LJKit_Tests' do
    inherit! :search_paths
  end
end
